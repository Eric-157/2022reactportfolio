export default function Nav() {
    return (
        <>
            {/* Home Page banner with multiple randomized on refresh images as
            the background to the banner title text */}
            <section id="banner"></section>
            {/* fixed nav so it is always accessible to the viewer */}
            <nav></nav>
        </>
    );
}
